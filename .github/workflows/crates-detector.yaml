name: Detect edited crates

on:
  workflow_call:
    inputs:
      base_ref:
        description: "Base branch to compare against"
        required: true
        type: string
    outputs:
      crates:
        description: "JSON array of edited crates"
        value: ${{ jobs.run-detector.outputs.crates }}
  workflow_dispatch:
    inputs:
      base_ref:
        description: "Base branch to compare against"
        required: true
        type: string

jobs:
  setup-detector:
    name: Setup Rust & Cache
    runs-on: ubuntu-latest
    outputs:
      cache-hit: ${{ steps.rust-cache.outputs.cache-hit }}
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Setup Rust & push cache
        id: rust-cache
        uses: ./.github/actions/rust-cache
        with:
          action: push
          packages: '["cargo-workspaces"]'
          crates: '["cargo-extract"]'

  run-detector:
    name: Detect Changed Crates
    runs-on: ubuntu-latest
    needs: setup-detector
    outputs:
      crates: ${{ steps.generate.outputs.crates }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Restore Rust cache
        uses: ./.github/actions/rust-cache
        with:
          action: pull
          packages: '["cargo-workspaces"]'
          crates: '["cargo-extract"]'

      - name: Fetch base ref
        env:
          BASE_REF: ${{ inputs.base_ref }}
        run: |
          git fetch origin "$BASE_REF" --no-tags

      - name: Run changed-crates detector
        id: detect
        run: |
          BASE_SHA=$(git rev-parse FETCH_HEAD)
          echo "Comparing against base SHA: $BASE_SHA"
          changed=$(cargo run --release -p cargo-extract -- "$BASE_SHA")
          echo "raw_list=$changed" >> "$GITHUB_OUTPUT"

      - name: Generate JSON crates output
        id: generate
        run: |
          raw_list="${{ steps.detect.outputs.raw_list }}"
          if [ -z "$raw_list" ]; then
            echo "crates=[]" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          crates=$(jq -c -Rn --arg csv "$raw_list" '$csv | split(" ")')
          echo "crates=$crates" >> "$GITHUB_OUTPUT"
